# Build output
bin/
dist/
# Legacy root-level binaries (build now outputs to bin/)
/auto-gmail
/gmail-mcp-server

# Claude Code session state
.claude/

# Test artefacts
coverage.out
coverage.html

# Environment / secrets
.env
*.env

# OAuth credentials and token — never commit these
credentials.json
token.json

# Editor
.vscode/
.idea/
